services:
  subscription-hub:
    build: .
    container_name: subscription-hub
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./data:/app/data
